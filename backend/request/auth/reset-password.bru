meta {
  name: Reset Password
  type: http
  seq: 9
}

post {
  url: {{baseUrl}}/auth/reset-password
  body: json
  auth: none
}

body:json {
  {
    "resetToken": "{{resetToken}}",
    "newPassword": "{{password}}"
  }
}

docs {
  see reset-password.md
}
